Replace redefine print with:

__builtins__['print'] = web_print

Same for input. Decorators for all functions. For scripts must be written manually,
already implemented.

Check if threading causes print bugs (same interpreter? Switch to multiprocessing?)

Add customizable input prompt for broswer_input.html

Move all config stuff to config dict

Remove all detectable stuff from autotest create_route calls

Check if detecting input is needed at all with __builtins__ method now used

Check if processes need to be terminated (with threading) - is_alive() useful for this?



